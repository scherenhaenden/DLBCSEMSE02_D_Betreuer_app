package com.example.betreuer_app.constants;

/**
 * Constants for authentication-related SharedPreferences keys and preference names.
 */
public class AuthConstants {

    /** The name of the SharedPreferences file for authentication data. */
    public static final String PREFS_NAME = "auth_prefs";

    /** Key for storing the JWT token. */
    public static final String KEY_JWT_TOKEN = "jwt_token";

    /** Key for storing the user's name. */
    public static final String KEY_USER_NAME = "user_name";

    /** Key for storing the user's role. */
    public static final String KEY_USER_ROLE = "user_role";
    
    /** Key for storing the user's ID. */
    public static final String KEY_USER_ID = "user_id";
}
